@model IEnumerable<PoliziaMunicipale.Data.ViewModels.TotaleVerbaliPerTrasgressoreViewModel>

@{
    ViewData["Title"] = "Totale Verbali per Trasgressore";
}

<h2>@ViewData["Title"]</h2>

<table class="table">
    <thead>
        <tr>
            <th>Trasgressore</th>
            <th>Totale Verbali</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.CognomeTrasgressore, @item.NomeTrasgressore</td>
                <td>@item.TotaleVerbali</td>
            </tr>
        }
    </tbody>
</table>
